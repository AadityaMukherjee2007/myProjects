CREATE TABLE profile(
	id SERIAL PRIMARY KEY,
	name VARCHAR(100),
	email VARCHAR(255),
	password TEXT,
	age INT
);

DROP TABLE profile;

CREATE TABLE "user"(
	id SERIAL PRIMARY KEY,
	name VARCHAR(100),
	email VARCHAR(255),
	password TEXT,
	age INT
);

INSERT INTO "user" (name, email, password, age) VALUES ('Harry', 'harry@example.com', 'fdsfdffaedf', 18);